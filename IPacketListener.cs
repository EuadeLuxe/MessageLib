namespace MessageLib
{
    /// <summary>
    /// The basic interface for packet listeners. It does not contains any functions on its
    /// own but is rather used for type hinting only.
    /// </summary>
    public interface IPacketListener
    {
        // No methods here; see PacketDispatcher for example uses.
    }
}
